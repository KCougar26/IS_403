<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= competition.Competition_ID ? 'Edit Competition' : 'Add Competition' %></title>
  
  <link rel="stylesheet" href="/css/styles.css"> 
</head>
<body class="ballroom-bg-management"> 
    
  <nav class="navbar">
    <div class="navbar-brand">Ballroom Admin</div>
    <div class="navbar-nav">
      <a class="nav-link" href="/users">Users</a>
      <a class="nav-link" href="/competitions">Competitions</a>
      <a class="nav-link" href="/events">Events</a>
      <a class="nav-link" href="/logout">Logout</a>
    </div>
  </nav>
  
  <main class="page-content-wrapper">
      
    <h1 class="page-title"><%= competition.Competition_ID ? 'Edit Competition' : 'Add Competition' %></h1>
    
    <form action="<%= action %>" method="POST" class="frosted-form-card">
      
      <div class="form-group-custom">
        <label class="form-label-custom">Location</label>
        <input class="input-field" name="location" value="<%= competition.Location || '' %>" required>
      </div>

      <div class="form-check-custom">
        <input class="check-input-custom" type="checkbox" name="sanctioned" <%= competition.Sanctioned ? 'checked' : '' %>>
        <label class="check-label-custom">Sanctioned Competition</label>
      </div>

      <button type="submit" class="view-btn save-btn-custom">Save</button>
      <a href="/competitions" class="action-btn btn-delete">Cancel</a> 
      
    </form>
  </main>
</body>
</html>