<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pokémon Search Result</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">

  <div class="container py-5">
    <h1 class="text-center mb-4">Pokémon Search Result</h1>

    <% if (pokemon) { %>
      <div class="card shadow mx-auto" style="max-width: 400px;">
        <div class="card-body text-center">
          <h3 class="card-title text-capitalize"><%= pokemon.description %></h3>
          <p class="card-text"><strong>Base Total:</strong> <%= pokemon.base_total %></p>
          <a href="/" class="btn btn-secondary mt-3">Back to List</a>
        </div>
      </div>
    <% } else { %>
      <div class="alert alert-warning text-center">
        No Pokémon found with the name "<%= query %>".
      </div>
      <div class="text-center">
        <a href="/" class="btn btn-secondary">Back to List</a>
      </div>
    <% } %>
  </div>

</body>
</html>