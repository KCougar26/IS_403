<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register - Workshop</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8 col-xl-7">
        
        <!-- Back Arrow -->
        <div class="mb-4">
          <a href="/" class="btn btn-link text-decoration-none fs-5 p-0">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 16 16">
              <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8z"/>
            </svg>
            <span class="ms-2">Back</span>
          </a>
        </div>

        <!-- Header -->
        <div class="text-center mb-5">
          <h1 class="display-5 fw-bold">Create Account</h1>
          <p class="text-muted fs-5">Fill in the information below to get started</p>
        </div>

        <!-- Main Card -->
        <div class="card border-0 shadow-lg">
          <div class="card-body p-5">
            <% if (error_message) { %>
              <div class="alert alert-danger mb-4" role="alert">
                <strong>Error:</strong> <%= error_message %>
              </div>
            <% } %>

            <form method="POST" action="/register">
              
              <!-- Account Credentials Section -->
              <div class="bg-light rounded-3 p-4 mb-4">
                <h5 class="border-bottom pb-3 mb-4">Account Credentials</h5>
                
                <div class="mb-4">
                  <label class="form-label fw-semibold">Username</label>
                  <input 
                    type="text" 
                    class="form-control form-control-lg" 
                    name="username" 
                    placeholder="Your username" 
                    required>
                </div>
                
                <div class="mb-0">
                  <label class="form-label fw-semibold">Password</label>
                  <input 
                    type="password" 
                    class="form-control form-control-lg" 
                    name="password" 
                    placeholder="Your password" 
                    required>
                </div>
              </div>

              <!-- Account Type Section -->
              <div class="bg-light rounded-3 p-4 mb-4">
                <h5 class="border-bottom pb-3 mb-4">Account Type</h5>
                
                <div class="border rounded-3 p-3 mb-3 bg-white">
                  <div class="form-check">
                    <input 
                      class="form-check-input" 
                      type="radio" 
                      name="role" 
                      id="roleUser" 
                      value="U" 
                      checked
                    >
                    <label class="form-check-label w-100" for="roleUser">
                      <div class="fw-semibold">User</div>
                      <small class="text-muted">Standard access with basic permissions</small>
                    </label>
                  </div>
                </div>

                <div class="border rounded-3 p-3 bg-white">
                  <div class="form-check">
                    <input 
                      class="form-check-input" 
                      type="radio" 
                      name="role" 
                      id="roleManager" 
                      value="M"
                    >
                    <label class="form-check-label w-100" for="roleManager">
                      <div class="fw-semibold">Manager</div>
                      <small class="text-muted">Administrative access with full permissions</small>
                    </label>
                  </div>
                </div>
              </div>

              <!-- Submit Button -->
              <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary btn-lg">
                  Create Account
                </button>
              </div>
            </form>

            <div class="text-center mt-4 pt-4 border-top">
              <p class="mb-0 text-muted">
                Already have an account? 
                <a href="/login" class="link-primary fw-semibold">Sign in here</a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>